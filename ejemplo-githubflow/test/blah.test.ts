import { substract, sum } from '../src';

describe('blah', () => {
  it('works', () => {
    expect(sum(1, 1)).toEqual(2);
  });
});

describe('blah', () => {
  it('works', () => {
    expect(substract(2, 1)).toEqual(1);
  });
});
